<h1>🗒️ To Do Manager ✅</h1>
<p>Choose a task for more actions</p>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task), class: "list" %></li>
  <% end %>
</ul>

<%= link_to "Create a new task", new_task_path, class: "button" %>
