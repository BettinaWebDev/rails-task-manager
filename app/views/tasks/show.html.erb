<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Back to tasks", tasks_path, class: "button" %>
<%= link_to "Edit task", edit_tasks_path, class: "button" %>
<%= link_to "Delete task",
  task_delete_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
